<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:jee="http://www.springframework.org/schema/jee"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
	   http://www.springframework.org/schema/beans/spring-beans.xsd
	   http://www.springframework.org/schema/context
	   http://www.springframework.org/schema/context/spring-context-3.2.xsd
       http://www.springframework.org/schema/jee
       http://www.springframework.org/schema/jee/spring-jee-3.2.xsd">

	<!-- Scan the com.spring.web.dao package and look for anything that is
	defined using annotations as a bean. Spring needs to know to load this
	XML file by adding some special entries to web.xml, the application's
	deployment descriptor. -->
	<context:annotation-config></context:annotation-config>
	<context:component-scan base-package="com.spring.web.dao">
	</context:component-scan>

    <jee:jndi-lookup jndi-name="jdbc/MySQLDataSource"
                     id="dataSource"
                     expected-type="javax.sql.DataSource">
    </jee:jndi-lookup>
</beans>
